# Update feed for `PGT_FORCE_UPDATE_CHECK=1` runs, so "Check for Updates" can be exercised
# outside a packaged build. Points at the same releases the packaged app reads.
provider: github
owner: sqwiro-opensource
repo: pgterminal
updaterCacheDirName: pgterminal-updater
